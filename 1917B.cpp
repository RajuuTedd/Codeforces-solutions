#include<bits/stdc++.h>
using namespace std; 
int main()
{
    vector <string> v;
    int t;
    cin>>t;
    while ((t--))
    {
        int n;
    string s;
    vector<char> V(s.begin(), s.end());
    n= s.length();
        cin>>s;
        v.erase(s.begin());
        for (int i = 0 ; i<s.length();i++)
        {

        }
    }
    
}